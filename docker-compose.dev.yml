# Development overrides — mounts local app/ so code changes take effect without rebuild.
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build

services:
  fastchannelsv2:
    volumes:
      - ./app:/app/app
